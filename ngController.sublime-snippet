<snippet>
    <content><![CDATA[(function() {
  'use strict';

  angular.module('${1:module}')
 	  .controller('${2:Controller}',
    /* @ngInject */
    function ${2:Controller}(
      ${3:dependencies}
    ) {
      var vm = this;
      vm.title = '${2:Controller}';
      activate();
      ////////////////
      function activate() {
      }
    }
  );
})();
]]></content>
    <tabTrigger>n-ctrl</tabTrigger>
    <scope>text.plain, source.js, source.ts</scope>
</snippet>
